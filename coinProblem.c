#include<stdio.h>
#include<stdlib.h>

int dp[1000][n];

int min(int a, int b) {
	return (a < b)?a:b;
}

void recur(int arr*, int n, int total) {
	if(n <= 0) return;
	if(total < 0) return;
	if(total == 0) {
		count = min(count, minVal);
	}
	int i;
	for(i = 0; i < n; i++) {
		recur(arr, n - 1, total - arr[i]);
		dp[]
	}
}

void coinPossible(int arr*, int n, int total) {
	int i;
	int maxSum = -1;
	
	int dp[total+1][n];


}

int main(void) {
	return 0;
}
